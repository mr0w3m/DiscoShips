using UnityEngine;
using System.Collections;

public class SaveLoadout : MonoBehaviour {

    //At the start of the game, save everyone's weapon & ability
    //if settings was selected
	//Don't destroy on load
    //When we get to the select screen
    //tell the game manager to set players loadouts
    //then destroy this obj
}
